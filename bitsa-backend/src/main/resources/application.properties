spring.application.name= BITSA Backend API

# Datasource (MySQL Workbench)
# Using the database you created with MySQL Workbench: bitsadb on localhost:3306
# NOTE: You used the root account for this DB. For development this works, but
# for better safety create a dedicated non-root user and update credentials.
spring.datasource.url=jdbc:mysql://root:HgfmicxaaLoiGQYiDqXPfoPvtPwpLqdZ@nozomi.proxy.rlwy.net:50877/railway?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&useUnicode=true&characterEncoding=utf8


spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:HgfmicxaaLoiGQYiDqXPfoPvtPwpLqdZ}
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=update

# JWT
security.jwt.secret=${JWT_SECRET:fTs6tdk6Uu78iUGchtcTMNpdPOUTUpXrSM12DzwTKHc=}
security.jwt.expiration-ms=${JWT_EXPIRATION:3600000}

# File upload
file.upload-dir=${UPLOAD_DIR:uploads}
file.upload.max-size=${MAX_FILE_SIZE:10MB}
file.upload.allowed-types=${ALLOWED_FILE_TYPES:image/jpeg,image/png,image/gif,image/webp}

# Server
server.port=${SERVER_PORT:8081}

# CORS
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:3000}
